.deploy-ssh:
  stage: deploy
  before_script:
    - apk add --no-cache openssh-client curl
    - eval $(ssh-agent -s)
    - SSH_PRIVATE_KEY=SSH_PRIVATE_KEY_${SERVICE_NAME} && eval cat "\$$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - echo "$SSH_KNOWN_HOSTS" >> ~/.ssh/known_hosts
    - chmod 644 ~/.ssh/known_hosts
  script:
    - !reference [".custom-vars"]
    - ssh gitlab-publish@ssh.crablr.io

.deploy-firebase:
  image: node:24.8.0-alpine3.22
  stage: deploy
  before_script:
    - apk update && apk add --no-cache curl python3
    - npm i -g firebase-tools@13.4.1
  script:
    - !reference [".custom-vars"]
    - cd examples
    - firebase --project $FIREBASE_PROJECT_ID deploy --only hosting:$SERVICE_NAME --token $FIREBASE_DEPLOY_TOKEN
